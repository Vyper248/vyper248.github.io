export const FRAME_SPEED = 15;

export const SPACE_WIDTH = window.innerWidth < 1500 ? 1500 : window.innerWidth;
export const SPACE_HEIGHT = window.innerHeight < 1000 ? 1000 : window.innerHeight;

export const GROUND_WIDTH = window.innerWidth < 1500 ? 1500 : window.innerWidth;
export const GROUND_HEIGHT = window.innerHeight < 1000 ? 1000 : window.innerHeight;