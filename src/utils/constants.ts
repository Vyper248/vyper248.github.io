export const FRAME_SPEED = 15;

export const SPACE_WIDTH = window.innerWidth < 1500 ? 1500 : window.innerWidth;
export const SPACE_HEIGHT = window.innerHeight < 1200 ? 1200 : window.innerHeight;

export const GROUND_WIDTH = window.innerWidth < 1800 ? 1800 : window.innerWidth;
export const GROUND_HEIGHT = window.innerHeight < 1500 ? 1500 : window.innerHeight;